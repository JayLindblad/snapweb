/**
 * Design tokens — change values here to restyle the whole app.
 * All numeric spacing values are MUI spacing units (1 unit = 8px by default).
 */

const tokens = {
  /** Padding around the edge of the page (cards to screen edge) */
  pagePadding: 1.5,

  /** Vertical gap between group cards */
  cardGap: 2,

  /** Horizontal padding inside a card */
  cardPaddingX: 2,

  /** Top/bottom padding inside a card */
  cardPaddingY: 1,

  /** Border radius of cards (px) — also set in the MUI theme */
  cardRadius: 20,

  /** Album art thumbnail size (px) */
  artSize: 64,

  /** Large play/pause button diameter (px) */
  playButtonSize: 52,

  /** Small skip-button icon size (px) */
  skipIconSize: 30,
};

export default tokens;
